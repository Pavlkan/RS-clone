const borderedItemStyles = {
  border: '1px solid',
  borderRadius: '10px',
};

const circleShape = {
  borderRadius: '50%',
  display: 'inline-block',
};

export { borderedItemStyles, circleShape };
